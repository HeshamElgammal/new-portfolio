/** @type {import('next').NextConfig} */
const nextConfig = {
  images: {
    domains: ['images.unsplash.com', 'via.placeholder.com', 'waseet.net', 'zeidex.com', 'media.licdn.com'],
  },
}

module.exports = nextConfig
